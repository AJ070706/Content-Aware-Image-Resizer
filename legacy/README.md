# Legacy interface

`main.py` is the earlier Tkinter interface. It is kept as a historical reference and is not imported by the React/pywebview desktop app or included in the current build. Active code lives in `src/` and `frontend/`.
